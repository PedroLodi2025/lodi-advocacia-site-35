Meu projeto web utiliza React + TypeScript + Wouter (frontend), com uma branch static que já inclui a configuração do Firebase (Authentication e Firestore) em firebase-config.js. O projeto foi adaptado para hospedagem estática na Hostinger, usando apenas URLs externas de imagens (conforme ATUALIZACAO-IMAGENS.md), e removeu uploads locais. No Replit, o ambiente foi configurado com sucesso: banco PostgreSQL criado, schema do Drizzle empurrado, aplicação reiniciada na porta 5000, dependências instaladas, e estrutura migrada. No entanto, ao acessar o painel de login na Hostinger, recebo um erro 404 HTTP, provavelmente porque o frontend ainda faz chamadas a rotas /api/* (ex.: /api/auth/signin) herdadas do backend Express.js, que não são suportadas em ambiente estático.
Por favor, ajuste o projeto para eliminar completamente a dependência do backend Express.js e do banco PostgreSQL/Drizzle, utilizando exclusivamente o Firebase já configurado para autenticação e gerenciamento de dados. As especificações são:

Desative o servidor Express.js (remova server/ e pare de escutar na porta 5000) e remova todas as chamadas a rotas /api/* no frontend.
Use o Firebase Authentication (signInWithEmailAndPassword) para o login no arquivo auth.js ou equivalente, redirecionando para /admin após sucesso e exibindo erros no console (ex.: console.error("Erro no login:", error.message)).
Migre o CRUD de artigos do painel admin (em admin.js ou articles.js) para usar Firestore, mantendo os campos existentes (title, description, category, image_url, etc.) e garantindo que apenas usuários autenticados (com role "admin") possam criar/editar/deletar.
Mantenha a estrutura atual do frontend (React, Wouter, Tailwind CSS, Shadcn/UI) e os arquivos estáticos em index.html, styles.css, etc.
Certifique-se de que o projeto seja 100% estático e compatível com a Hostinger após um build com vite build, gerando a pasta dist para upload via File Manager.
Valide as credenciais do firebase-config.js no console do Firebase (firebase.google.com) e corrija se necessário.
Adicione regras de segurança no Firebase Firestore para permitir leitura pública de artigos e escrita/restrita apenas a admins autenticados: